{
  "name": "doner-dash",
  "version": "1.0.0",
  "description": "Döner Dash — Kebab Chaos browser game",
  "private": true,
  "scripts": {
    "test": "jest --verbose",
    "test:ci": "jest --verbose --ci --coverage"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0"
  }
}
